### File managed with puppet ###
#
## Served by:        '<%= scope.lookupvar('::servername') %>'
## Module:           '<%= scope.to_hash['module_name'] %>'
## Template source:  '/etc/puppet/environments/production/modules/snmpd/templates/snmpd.conf.erb'
#

sysName <%= scope.lookupvar('snmpd::snmpname') %>
sysLocation <%= scope.lookupvar('snmpd::snmplocation') %>
sysContact <%= scope.lookupvar('snmpd::snmpcontact') %>

# The following is generated from snmpd::options
<% scope.lookupvar('snmpd::options').sort.each do |option| %><%= option.first %> <%= option.last %> 
<% end %>
